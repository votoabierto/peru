/**
 * Single source of truth for quiz configuration.
 * Update here when question count changes — never hardcode elsewhere.
 */
export const QUIZ_QUESTION_COUNT = 13
export const QUIZ_THEME_COUNT = 3
export const QUIZ_TIME_MINUTES = 5

/** Human-readable tagline used in metadata / copy across the site. */
export const QUIZ_META_TAGLINE = `${QUIZ_QUESTION_COUNT} preguntas, ${QUIZ_TIME_MINUTES} minutos, 100% anónimo.`
